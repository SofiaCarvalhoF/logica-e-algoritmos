// Importa o módulo (Para rodar algoritmo no terminal)
const prompt = require('prompt-sync')()

// Título
console.log('--- Fogo ---')
console.log("")

// Loop de 10 até 1 (passo -1)
for (let contador = 10; contador >= 1; contador--) {
    console.log(contador);
}

// Mensagem final após o término do loop
console.log("Fogo!");